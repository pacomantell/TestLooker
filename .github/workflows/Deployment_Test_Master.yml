name: deployment_test_prod

on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Copy file
        run: |
          cp .github/workflows/src.yaml .github/workflows/dest.yaml
