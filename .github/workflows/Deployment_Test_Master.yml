name: deployment_test_prod

on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Copy file
        run: |
          cp models/formacion_analytics.model.lkml models/formacion_analytics.model.lkml
