name: deployment_test_prod

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Copy file
        run: |
          cp models/formacion_analytics_test.model.lkml models/formacion_analytics.model.lkml
            - name: Copy file
      - name: Remove file
        run: |
          rm models/formacion_analytics_test.model.lkml
